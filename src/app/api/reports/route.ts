import {NextResponse} from 'next/server';import {requireRole} from '@/lib/auth';import {prisma} from '@/lib/prisma';export async function GET(){await requireRole(['ADMIN','VIEWER']);const rows=await prisma.order.findMany({where:{status:'COMPLETED'},include:{payment:true},orderBy:{completedAt:'desc'}});const csv=['Order ID,Date,Payment,Total',...rows.map(o=>`${o.code},${o.completedAt?.toISOString()||''},${o.payment?.method||''},${o.total.toFixed(2)}`)].join('\n');return new NextResponse(csv,{headers:{'content-type':'text/csv','content-disposition':'attachment; filename="ice-it-up-sales.csv"'}})}
