'use client'
import {useState} from 'react';import {useRouter} from 'next/navigation';import {Eye,EyeOff,Snowflake} from 'lucide-react'
export default function Login(){const r=useRouter();const[email,setEmail]=useState('');const[pw,setPw]=useState('');const[show,setShow]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password:pw})});const d=await res.json();if(!res.ok){setError(d.error||'Unable to sign in.');setBusy(false);return}r.push('/dashboard');r.refresh()}return <main className="min-h-screen grid lg:grid-cols-2 bg-[#f7fbff]"><section className="hidden lg:flex bg-[#eaf6ff] items-center justify-center p-16"><div className="max-w-md"><div className="h-16 w-16 rounded-2xl bg-[#bfe8ff] grid place-items-center mb-8"><Snowflake/></div><p className="text-sm font-semibold tracking-[.22em] text-sky-700">ICE IT UP</p><h1 className="text-5xl font-bold tracking-tight mt-3">Run the business.<br/>Keep it cool.</h1><p className="mt-5 text-slate-600 text-lg">Sales, orders, inventory, payments and reports in one focused workspace.</p></div></section><section className="flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-sm"><div className="lg:hidden h-14 w-14 rounded-2xl bg-[#d9f1ff] grid place-items-center mb-7"><Snowflake/></div><p className="text-sm font-semibold text-sky-700 tracking-widest">MARK'S ICE IT UP</p><h2 className="text-3xl font-bold mt-2">Welcome back</h2><p className="text-slate-500 mt-2 mb-8">Sign in to your ICE IT UP account.</p><label className="label">Email</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/><label className="label mt-5">Password</label><div className="relative"><input className="input pr-12" type={show?'text':'password'} required value={pw} onChange={e=>setPw(e.target.value)} placeholder="••••••••"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400">{show?<EyeOff size={19}/>:<Eye size={19}/>}</button></div>{error&&<div className="mt-4 rounded-xl bg-red-50 text-red-700 px-4 py-3 text-sm">{error}</div>}<button disabled={busy} className="btn-primary w-full mt-6">{busy?'Signing in…':'Sign in'}</button></form></section></main>}
