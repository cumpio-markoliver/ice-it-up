'use client'
import {useState} from 'react';import {useRouter} from 'next/navigation';import {Eye,EyeOff,Snowflake,ShieldCheck} from 'lucide-react'
export default function Login(){const r=useRouter();const[email,setEmail]=useState('');const[pw,setPw]=useState('');const[show,setShow]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password:pw})});const d=await res.json();if(!res.ok){setError(d.error||'Unable to sign in.');setBusy(false);return}r.replace('/dashboard');r.refresh()}catch{setError('Unable to connect right now. Please try again.');setBusy(false)}}return <main className="login-page"><section className="login-visual"><div className="relative z-10 max-w-xl"><div className="login-logo"><Snowflake size={31}/></div><p className="mt-8 text-sm font-black tracking-[.22em] text-sky-700">ICE IT UP</p><h1 className="mt-3 text-5xl font-black tracking-tight text-slate-900">Run the business.<br/>Keep it cool.</h1><p className="mt-6 max-w-lg text-lg leading-8 text-slate-600">Sales, orders, inventory, payments and reports in one focused workspace.</p><div className="mt-8 flex items-center gap-2 text-sm font-semibold text-slate-600"><ShieldCheck size={18} className="text-sky-600"/> Secure role-based access</div></div></section><section className="login-card"><form onSubmit={submit} className="login-form panel"><div className="lg:hidden login-logo mb-7"><Snowflake size={28}/></div><p className="text-sm font-black tracking-[.18em] text-sky-700">ICE IT UP</p><h2 className="mt-2 text-3xl font-black">Welcome back</h2><p className="mt-2 mb-8 text-sm text-slate-500">Sign in to your authorized account.</p><label className="label">Email</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" autoComplete="email"/><label className="label mt-5">Password</label><div className="relative"><input className="input pr-12" type={show?'text':'password'} required value={pw} onChange={e=>setPw(e.target.value)} placeholder="Enter your password" autoComplete="current-password"/><button type="button" aria-label={show?'Hide password':'Show password'} onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400">{show?<EyeOff size={19}/>:<Eye size={19}/>}</button></div>{error&&<div role="alert" className="mt-4 rounded-xl border border-red-200 bg-red-50/80 px-4 py-3 text-sm text-red-700">{error}</div>}<button disabled={busy} className="btn-primary w-full mt-6">{busy?'Signing in…':'Sign in'}</button><p className="mt-6 text-center text-xs text-slate-400">Authorized ICE IT UP accounts only.</p></form></section></main>}
