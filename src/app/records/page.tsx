import {AppShell} from '@/components/app-shell';import {requireUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';export default async function Records(){await requireUser();const orders=await prisma.order.findMany({where:{status:'COMPLETED'},include:{payment:true},orderBy:{completedAt:'desc'},take:100});return <AppShell><p className="text-sm font-bold text-sky-700">Daily Records</p><h1 className="text-3xl font-black mt-1">Completed sales</h1><section className="panel mt-6 overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50"><tr><th className="p-4 text-left">Order</th><th className="p-4 text-left">Date</th><th className="p-4 text-left">Payment</th><th className="p-4 text-right">Total</th></tr></thead><tbody className="divide-y">{orders.map(o=><tr key={o.id}><td className="p-4 font-semibold">{o.code}</td><td className="p-4">{o.completedAt?.toLocaleString()}</td><td className="p-4">{o.payment?.method}</td><td className="p-4 text-right font-bold">₱{o.total.toFixed(2)}</td></tr>)}</tbody></table></section></AppShell>}
